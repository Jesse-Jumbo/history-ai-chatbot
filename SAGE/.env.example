# =============================================================================
# SAGE 環境變數設定範例
# 複製此檔案為 .env 並根據需要修改
# =============================================================================

# -----------------------------------------------------------------------------
# 模式設定
# -----------------------------------------------------------------------------
# 強制 Mock 模式 (true/false)
# 開啟後將使用 CPU 影像處理，不使用 SAM 模型
SAGE_MOCK_MODE=false

# 自動 Mock 模式 (true/false)
# 開啟後在沒有 GPU 時自動切換到 Mock 模式
SAGE_AUTO_MOCK=false

# -----------------------------------------------------------------------------
# 年齡設定
# -----------------------------------------------------------------------------
# 預設目標年齡 (0-100)
SAGE_TARGET_AGE=75

# -----------------------------------------------------------------------------
# 攝影機設定
# -----------------------------------------------------------------------------
# 攝影機索引 (0, 1, 2, ...)
SAGE_CAMERA_INDEX=0

# 攝影機解析度
SAGE_CAMERA_WIDTH=1280
SAGE_CAMERA_HEIGHT=720

# -----------------------------------------------------------------------------
# API 伺服器設定
# -----------------------------------------------------------------------------
# API 主機 (0.0.0.0 允許外部連線，127.0.0.1 只允許本機)
SAGE_API_HOST=0.0.0.0

# API 埠號
SAGE_API_PORT=8000

# 開發模式自動重載 (true/false)
SAGE_API_RELOAD=false

# -----------------------------------------------------------------------------
# 日誌設定
# -----------------------------------------------------------------------------
# 日誌等級 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
SAGE_LOG_LEVEL=INFO
